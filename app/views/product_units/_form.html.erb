<%= form_for(@product_unit) do |f| %>
  <% if @product_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_unit.errors.count, "error") %> prohibited this product_unit from being saved:</h2>

      <ul>
      <% @product_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_unit_name %><br>
    <%= f.text_field :product_unit_name %>
  </div>
  <div class="field">
    <%= f.label :product_unit_abbreviation %><br>
    <%= f.text_field :product_unit_abbreviation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
